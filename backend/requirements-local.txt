# Local development requirements
# Uses flexible versions compatible with Python 3.13

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pillow>=10.2.0
torch>=2.4.0          # 2.4+ supports Python 3.13
torchvision>=0.19.0
numpy>=1.26.3
opencv-python-headless>=4.9.0.80
sqlalchemy>=2.0.25
pydantic>=2.5.3
python-dotenv>=1.0.0
boto3>=1.34.34
httpx>=0.26.0

# psycopg2-binary omitted locally — we use SQLite instead
# gradio-client omitted locally — not needed
